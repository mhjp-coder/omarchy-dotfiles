general {
    lock_cmd = pidof hyprlock || hyprlock                  # lock screen
    before_sleep_cmd = loginctl lock-session               # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on             # to avoid having to press a key twice to turn on the display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

{{ if eq .machine_location "home" -}}

listener {
    timeout = 900                                            # 15min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}

listener {
    timeout = 1800                                           # 30min
    on-timeout = loginctl lock-session                       # lock screen when timeout has passed
}

listener {
    timeout = 3600                                          # 60min
    on-timeout = systemctl suspend                          # suspend when timeout has passed
}

{{ end -}}


{{ if eq .machine_location "work" -}}

listener {
    timeout = 900                                            # 15min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}

listener {
    timeout = 1200                                           # 20min
    on-timeout = loginctl lock-session                       # lock screen when timeout has passed
}

listener {
    timeout = 3600                                          # 60min
    on-timeout = systemctl suspend                          # suspend when timeout has passed
}

{{ end -}}


{{ if eq .machine_location "framework" -}}

listener {
    timeout = 200                                           # 3min
    on-timeout = brightnessctl -s set 20                    # dim screen when timeout has passed
    on-resume = brightnessctl -r                            # restore brightness when activity is detected
}

listener {
    timeout = 200                                            # 3min
    on-timeout = brightnessctl -sd rgb:kbd_backlight set 0   # turn off keyboard backlight when timeout has passed
    on-resume = brightnessctl -rd rgb:kbd_backlight         # restore keyboard backlight when activity is detected
}

listener {
    timeout = 300                                            # 5min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}

listener {
    timeout = 600                                            # 10min
    on-timeout = loginctl lock-session                       # lock screen when timeout has passed
}

listener {
    timeout = 1200                                          # 20min
    on-timeout = systemctl suspend                          # suspend when timeout has passed
}

{{ end -}}
